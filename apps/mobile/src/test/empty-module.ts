// Empty stand-in for side-effect-only native modules (e.g.
// `react-native-url-polyfill/auto`) that expect the React Native bridge.
// Node already provides the globals these polyfills install.
export {};
