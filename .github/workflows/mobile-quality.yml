name: Mobile quality

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  mobile-quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint mobile workspace
        run: npm run lint

      - name: Typecheck mobile workspace
        run: npm run typecheck
